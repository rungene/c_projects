#include <stdio.h>
/**
 * main -program entry point
 * for -loop
 * @a: First operand
 * @b: Second operand
 *
 * Return: The function return zero
 */
int main(void)
{
	int a, b;

	for (a = 0; a < b; a++)
	{
	printf("%d\n", a);
	}
	return (0);

}

